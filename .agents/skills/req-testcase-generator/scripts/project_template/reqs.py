# -*- coding: utf-8 -*-
"""需求原子化清单 —— 项目数据层，每个新项目重写本文件。

字段序固定（tcgen.audit 按位置取值，不要改顺序）：
    (REQ-ID, 需求描述, 来源文档, 来源章节/段落, 类型, 可测性, 覆盖状态, 备注)

枚举约束（违反会被门禁「REQ清单结构性完整」抓出）：
    类型     只能是 功能 / 性能 / 稳定性 / 兼容性 / 安全 / 用户体验
             安全类一律填「安全」，子域写进备注（信息安全 / 功能安全）。
             严禁把类型填成「功能安全」——含「功能」二字会被误判为功能类 REQ，
             叠加「正向+反向」深度要求，造成双重判定。
    可测性   只能是 可测 / 不可测
    覆盖状态 只能是 待覆盖 / 阻塞 / N/A（待覆盖的会在构建时按用例自动改为已覆盖）

编号约束：REQ-001 起连续，不得断号重号；不可测项用 REQ-N01 起单独编号。
来源章节必须与 d_ex.DOC_SECTIONS 里的章节字符串完全一致，否则章节反查会报缺口。
"""

REQ_SRC = [
    # ---- 可测需求 ----
    ("REQ-001", "示例：点击【保存】使当前配置生效", "软件PRD", "4.1.1 F01",
     "功能", "可测", "待覆盖", "-"),
    ("REQ-002", "示例：未选择必填项时不能保存", "软件PRD", "4.1.1 F01",
     "功能", "可测", "待覆盖", "-"),
    ("REQ-003", "示例：单次操作响应<2s", "软件PRD", "4.1.2 F02",
     "性能", "可测", "待覆盖", "专项"),
    # 功能安全类必须标子域，且须有触发+恢复两类覆盖（门禁15）
    ("REQ-004", "示例：急停触发后整机停止与解除恢复", "异常注入库", "EX-001",
     "安全", "可测", "待覆盖", "功能安全;三触发源各须≥1条;停止时限X待安规给值"),
    # ---- 不可测/本期不做 ----
    ("REQ-N01", "示例：本期不做的功能", "软件PRD", "3.4版本规划①",
     "功能", "不可测", "N/A", "本期不做(Q1)"),
]

# 顶部「输入来源」合并行：第一项是标题，其余每项一行
REQ_SRC_LINKS = [
    "输入来源",
    "需求文档(飞书)XXX：https://example.feishu.cn/wiki/xxxx",
    "参考文档：xxx；原型：xxx",
]

# 仅由专项用例覆盖的 REQ（不参与功能深度判定，另走「专项+配套功能用例」规则）
SPEC_REQS = {'REQ-003'}

# 追溯矩阵里追加的专项行：(REQ-ID, TP-ID, TC-ID, 覆盖类型, 备注)
SPEC_TR_ROWS = [
    ('REQ-003', 'TP-P-001', 'TC-SP-PERF-001', '专项', '专项指标采样'),
]

# 审计报告顶部说明
REPORT_NOTE = ('报告说明', [
    '1. 本报告全部门禁数由脚本遍历数据算出，违例项列出具体 ID，不手填 0。',
    '2. 分母口径：可测∧非阻塞 REQ；阻塞与 N/A 单列不计入覆盖率分母。',
    '3. Blocked 用例均在附表「解除条件」列写明转 Ready 条件，非静默挂起。',
])
