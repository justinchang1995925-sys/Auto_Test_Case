# 本仓库是这套 Skill 的唯一来源，skills/ 下全部内容都要入库。
# 下面只忽略「可再生」的东西。

# Python 字节码（跑管道自检时生成）
__pycache__/
*.pyc

# 交付件产物（由 skills/req-testcase-generator/scripts/project_template/build.py 生成）
测试交付件-*.xlsx
*测试点.drawio
.feishu_csv/

# 管道自检临时目录
_selftest/

# 项目侧本地配置：写着某台机器上的 skill 绝对路径，不应入库
.tcgen_home

# 编辑器与工具的本地目录（若在本仓库内开工作区时产生）
.claude/settings.local.json
